class TaskManager {
  constructor(currentId = 0) {
    this.tasks = [];
    this.currentId = currentId;
  }
  addTask(taskName, assignedTo, dueDate, taskNotes){
    this.currentId++;
    tasks.push(id, taskName, assignedTo, dueDate, taskNotes);
  }


}
